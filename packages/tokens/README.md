# @varde/tokens

Design tokens for Varde. Processed by [Terrazzo](https://terrazzo.app) into CSS custom properties for dark and light themes, output to `dist/themes.css`.

## Commands

```sh
bun run build             # Run Terrazzo — generate dist/themes.css
bun run lint              # Validate all token references
```
